<div class="<%= message.role == 'user' ? 'text-right' : 'text-left' %>">
  <div class="<%= message.role == 'user' ? 'bg-blue-500 text-white' : 'bg-gray-200' %> inline-block px-4 py-2 rounded-lg mb-2">
    <%= message.content %>
  </div>
</div>
