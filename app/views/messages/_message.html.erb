<div class="<%= message.role == 'assistant' ? 'bg-gray-200' : 'bg-blue-100' %> p-4 rounded-xl shadow-sm">
  <strong class="block mb-1 text-gray-700"><%= message.role.capitalize %>:</strong>
  <p class="text-gray-900 whitespace-pre-line"><%= message.content %></p>
</div>
