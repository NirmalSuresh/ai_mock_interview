<%= turbo_frame_tag "new_message" do %>
  <%= form_with model: [session, Message.new],
                data: { turbo_stream: true },
                class: "flex gap-3 p-3 border rounded bg-gray-50 shadow-md" do |f| %>

    <%= f.text_area :content,
        placeholder: "Type your answer…",
        rows: 2,
        class: "flex-grow border p-3 rounded-lg resize-none text-base",
        id: "message_input" %>

    <%= f.submit "Send",
        class: "bg-blue-600 hover:bg-blue-700 text-white px-5 py-3 rounded-lg font-semibold" %>

  <% end %>
<% end %>
