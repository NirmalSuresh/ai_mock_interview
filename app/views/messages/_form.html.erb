<div id="message_form" class="w-full mt-4">
  <%= form_with model: [session, Message.new], data: { turbo_stream: true }, html: { class: "flex gap-3 w-full" } do |f| %>

    <%= f.text_area :content,
      rows: 2,
      placeholder: "Type your answer… (type 'end' to finish)",
      class: "flex-grow p-3 border rounded-xl bg-gray-50 focus:bg-white focus:outline-none resize-none text-lg" %>

    <%= f.submit "Send",
      class: "px-6 py-3 bg-blue-600 text-white rounded-xl hover:bg-blue-700 text-lg font-semibold" %>

  <% end %>
</div>
