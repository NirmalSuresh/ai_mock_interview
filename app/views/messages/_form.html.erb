<%= form_with model: [session, Message.new],
              data: { turbo_stream: true },
              html: { class: "d-flex align-items-center gap-2 w-100", multipart: true } do |f| %>

  <%= f.text_field :content,
      id: "message_content",
      placeholder: "Aa",
      data: { "message-form-target": "input" },
      class: "form-control rounded-pill px-4 py-2" %>

  <%= f.file_field :attachment, class: "d-none", id: "fileUpload" %>

  <label for="fileUpload" class="btn btn-light rounded-circle shadow-sm">
    📎
  </label>

  <%= f.submit "➤",
        class: "btn btn-primary rounded-circle px-3 py-2 fw-bold" %>

<% end %>
