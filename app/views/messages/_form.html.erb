<%= turbo_frame_tag "new_message" do %>
  <%= form_with(
        url: assistant_session_messages_path(session),
        method: :post,
        data: { turbo_stream: true, turbo: true },
        class: "flex gap-3 p-4 sticky bottom-0 bg-white border-t shadow-lg"
      ) do |f| %>

    <%= text_area_tag :content, "",
          placeholder: "Type your answer…",
          rows: 2,
          class: "flex-grow border p-3 rounded-xl resize-none text-lg",
          id: "chat_input" %>

    <%= submit_tag "Send",
          class: "bg-blue-600 text-white px-5 py-3 rounded-xl font-bold hover:bg-blue-700" %>

  <% end %>
<% end %>
