<!-- TIMER -->
<div data-controller="timer"
     data-timer-end-time-value="<%= @session.ends_at.to_i %>"
     class="mb-4 text-right text-lg fw-bold">
  Time Left: <span data-timer-target="output">--:--</span>
</div>

<h1 class="text-2xl font-bold mb-4">
  Interview for <%= @session.role %> — Question <%= @session.current_question_number %>/25
</h1>

<div id="messages"
     data-controller="autoscroll"
     class="space-y-3 max-h-[70vh] overflow-y-auto p-2 border rounded">
  <%= render @session.messages %>
</div>

<%= render "messages/form", session: @session %>
